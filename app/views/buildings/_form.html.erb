<%= form_with(model: building) do |form| %>
  <% if building.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(building.errors.count, 'error impidió', plural: 'errores impidieron') %> que este edificio se guardara:</h2>

      <ul>
        <% building.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :nombre, class: 'form-label' %>
    <%= form.text_field :nombre, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :direccion, class: 'form-label' %>
    <%= form.text_field :direccion, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :ciudad, class: 'form-label' %>
    <%= form.text_field :ciudad, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit :class => 'btn btn-outline-success' %>
  </div>
<% end %>
